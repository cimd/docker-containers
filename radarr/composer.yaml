services:
  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    volumes:
      - /opt/docker/radarr/data:/config
      - /opt/docker/radarr/movies:/movies #optional
      - /opt/docker/radarr/download-client-downloads:/downloads #optional
    ports:
      - 7878:7878
    restart: unless-stopped
